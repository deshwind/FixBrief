targets:
  $default:
    builders:
      json_serializable:
        options:
          checked: true
          explicit_to_json: true
          field_rename: snake
          include_if_null: false
      source_gen:combining_builder:
        options:
          preamble: // coverage:ignore-file

